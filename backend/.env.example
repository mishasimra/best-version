PORT=5000
NODE_ENV=development
CLIENT_URL=http://127.0.0.1:4173,http://localhost:4173,https://your-frontend-domain.vercel.app
# Frontend base URL used to generate reset-password links.
APP_BASE_URL=http://127.0.0.1:4173
MONGO_URI=mongodb://127.0.0.1:27017/best-version
USE_IN_MEMORY_DB=false
AUTO_SEED_DEMO=false
JWT_SECRET=replace-with-a-secure-secret
JWT_EXPIRES_IN=7d
# Local/dev defaults can leave this true. On deployed testing, set this to true to expose a clickable reset link in the UI.
ENABLE_DEV_RESET_LINK=true
# Optional SMTP settings for real password reset email delivery.
MAIL_FROM=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
GOOGLE_CLIENT_ID=replace-with-your-google-web-client-id
GOOGLE_CLIENT_SECRET=replace-with-your-google-web-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5000/api/auth/google/callback
